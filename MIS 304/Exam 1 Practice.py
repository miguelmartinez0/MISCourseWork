#Get order from user
order = int(input("Hello! What would you like to order? #"))
#Make sure order is within range.
while order > 0 or order < 6:
    #Input validation. Prompt user again.
    order = int(input("Number must be between 1 and 5. Please try again. #"))
while order > 0 and order < 5:
    #logic to calculate price
    #logic to calulcate quantity
    #logic to calcualte weight
    #Get order from user
    #Make sure order is within range
